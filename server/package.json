{
  "name": "become-op-server",
  "version": "1.0.0",
  "description": "Multiplayer WebSocket backend for Become OP progressive game",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
